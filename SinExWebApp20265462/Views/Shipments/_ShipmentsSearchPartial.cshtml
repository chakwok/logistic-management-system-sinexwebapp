@model SinExWebApp20265462.ViewModels.ShipmentsSearchViewModel

@if (User.IsInRole("Customer"))
{
    <text> Shipping Account : </text>
    @Html.DisplayFor(a => a.ShippingAccountId)
}


<text> Account Type : </text>
@Html.LabelFor(a => a.AccountType, "")


@using (Html.BeginForm("GenerateHistoryReport", "Shipments", "FormMethod.Get"))
{
    //@Html.DropDownListFor(a => a.ShippingAccountId, Model.ShippingAccounts, "Please select")
    //<p></p>
    <p>
        Shipped date from&nbsp
        @Html.TextBoxFor(a => a.StartShippedDate)
        &nbspto&nbsp
        @Html.TextBoxFor(a => a.EndShippedDate)
    </p>
    <p></p>
    <input type="submit" value="Search" />
    <p></p>
}